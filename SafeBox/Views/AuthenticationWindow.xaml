<Window x:Name="Window" x:Class="SafeBox.Views.AuthenticationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lb="clr-namespace:SafeBox.Behaviors"
        xmlns:conv="clr-namespace:SafeBox.Converters"
        xmlns:vm="clr-namespace:SafeBox.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:AuthenticationViewModel}"
        mc:Ignorable="d"
        Title="SafeBox Authentication" Height="351" Width="499"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanMinimize"
        Background="{x:Null}"
        Icon="/SafeIcon.ico"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <vm:AuthenticationViewModel x:Key="AuthenticationViewModel" />
        <conv:SecureStringToBooleanConverter x:Key="SecureStringToBooleanConverter" />

        <Storyboard x:Key="LoadingStoryboard">
            <DoubleAnimation
                Storyboard.TargetName="Window"
                Storyboard.TargetProperty="Opacity"
                From="0"
                To="1"
                Duration="0:0:0.150"/>
        </Storyboard>

        <Storyboard x:Key="ClosingStoryboard">
            <DoubleAnimation
                Storyboard.TargetName="Window"
                Storyboard.TargetProperty="Opacity"
                From="1"
                To="0"
                Duration="0:0:0.200"/>
        </Storyboard>
    </Window.Resources>
    
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:ControlStoryboardAction Storyboard="{StaticResource LoadingStoryboard}" ControlStoryboardOption="Play" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <b:Interaction.Behaviors>
        <lb:WindowClosingBehavior Storyboard="{StaticResource ClosingStoryboard}" />
        <lb:BorderlessWindowDragBehavior />
    </b:Interaction.Behaviors>

    <Border Margin="5">
        <Grid>
            <StackPanel Margin="0,0,0,64" >
                <TextBlock
                    TextWrapping="Wrap"
                    Text="Hello. Before we continue, you need to enter a backup password if it was set previously or set a new one.&#x0a;&#x0a;Keep in mind that the password will be used to retrieve access to accounts and protect your data. If you have already set a password, you will need to repeat that password to recover your existing accounts."
                    FontFamily="Calibri" FontSize="16"
                    Foreground="LightGray"
                    Padding="30,0,30,0"
                    Margin="0,30,0,20" Opacity="0.9" HorizontalAlignment="Center"/>

                <PasswordBox
                    Style="{StaticResource MaterialDesignFloatingHintRevealPasswordBox}"
                    md:TextFieldAssist.HasLeadingIcon="True"
                    md:TextFieldAssist.LeadingIcon="FormTextboxPassword"
                    md:HintAssist.Foreground="White"
                    md:TextFieldAssist.HasClearButton="True"
                    md:HintAssist.Hint="Enter a password..."
                    Width="220"
                    Foreground="White"
                    Margin="0,10,0,5">
                    <b:Interaction.Behaviors>
                        <lb:PasswordBoxSecureBehavior SecurePassword="{Binding Password, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                    </b:Interaction.Behaviors>
                </PasswordBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,282,0,0" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button
                    FocusVisualStyle="{x:Null}"
                    Content="Exit"
                    Width="120" Height="30"
                    Margin="0,0,10,15"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="Click">
                            <b:CallMethodAction
                                MethodName="Close"
                                TargetObject="{Binding ElementName=Window}" />
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                </Button>

                <Button
                    FocusVisualStyle="{x:Null}"
                    Content="Continue..."
                    Margin="0,0,25,15"
                    Width="120" Height="30"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button.IsEnabled>
                        <Binding
                            Converter="{StaticResource SecureStringToBooleanConverter}"
                            Path="Password" />
                    </Button.IsEnabled>
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="Click">
                            <b:InvokeCommandAction Command="{Binding ValidateCommand}" />
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                </Button>
            </StackPanel>
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF4A278A"/>
                    <GradientStop Color="#FF241048" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Grid.Effect>
                <DropShadowEffect Color="#FFBC9FFD" ShadowDepth="0" BlurRadius="15"/>
            </Grid.Effect>
        </Grid>
    </Border>
</Window>
